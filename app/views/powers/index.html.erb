<h2>All Superpowers</h2>

<ul>
  <% @powers.each do |power| %>
    <li>
      <%= power.name %>
      <br>
      Description: <%= power.description %>
      <br>
      Heroines with power: <%= HeroinePower.where(power_id: power.id).count %>
    </li>
    <br>
  <% end %>
</ul>
