<h2>All Superpowers</h2>

<ul>
  <% @powers.each do |p| %>
    <li>
      <%= link_to p.name, power_path(p.id) %>
      <br>
      Description: <%= p.description %>
      <br>
      Number of Heroines: <%= p.heroines.count %>
    </li>
    <br>
  <% end %>
</ul>
